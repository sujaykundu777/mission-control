# Day 1 Plan

Part 1 :

1. Create a Import Button with dropdown with 2 options - Import via CSV and Import via Json

2. Import Dropdown

- when clicked on the Import via CSV, a modal will popup for Import Via CSV

- when clicked on the Import via JSON, a modal will popup for Import via JSON

3. Drag and Drop Upload Modals

- In the Import via CSV modal, create a dropzone using react-dropzone, where it accepts to select only csv files

- In the Import via JSON modal, create a dropzone using react-dropzone, where it accepts to select only json files

- Create a upload button to trigger the upload logic

Part 2:

1. Create a preview functionality for the selected csv, when clicked on the preview option it will show the table of the csv data, so that user can see it before starting the upload process

2. Based on the Export CSV Template Create a 1000 contacts sample import csv (with large data for importing) with random details to test the import functionality.

Part 3:

1. Create a pagination functionality for the contacts list, add pagination selectors below, per page 10,20,50, 100 options and next previous page buttons in the footer properly aligned.

2. Fix the navigation sidebar so that it doesnt scroll up when the list is huge..
